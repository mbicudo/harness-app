apiVersion: apps/v1
kind: Deployment
metadata:
  name: harness-app
  labels:
    app: harness-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: harness-app
  template:
    metadata:
      labels:
        app: harness-app
    spec:
      containers:
        - name: harness-app
          # Use your Docker Hub image, e.g. YOUR_DOCKERHUB_USER/harness-app:latest
          # imagePullPolicy: Always required for multi-replica; IfNotPresent only safe for single-node (e.g. Minikube)
          image: mddb/harness-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
